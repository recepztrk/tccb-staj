@{
    ViewData["Title"] = "Email Gönderildi";
    Layout = "_LoginLayout";
}

<div class="card email-sent-card shadow">
    <div class="card-body text-center">
        <div class="email-sent-icon">
            <i class="fas fa-envelope-open fa-4x"></i>
        </div>
        
        <h2 class="email-sent-title">Email Gönderildi!</h2>
        
        @if (TempData["Success"] != null)
        {
            <div class="alert email-sent-alert">
                <i class="fas fa-check-circle me-2"></i> @TempData["Success"]
            </div>
        }
        
        <p class="text-muted mb-4">
            Email adresinize doğrulama linki gönderildi. 
            Gelen kutunuzu (ve spam klasörünüzü) kontrol edin.
        </p>

        <div class="email-sent-info-box mb-4">
            <h6 class="mb-3">📧 Email aldınız mı?</h6>
            <div class="text-muted small">
                <div class="mb-1">• Gelen kutuyu kontrol edin</div>
                <div class="mb-1">• Spam/çöp kutusu kontrolü yapın</div>
                <div>• Link 24 saat geçerlidir</div>
            </div>
        </div>

        <div class="d-grid gap-3">
            @if (!string.IsNullOrEmpty(ViewBag.Email as string))
            {
                <form asp-action="ResendVerificationEmail" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="email" value="@ViewBag.Email" />
                    <button type="submit" class="btn btn-outline-primary email-sent-btn">
                        <i class="fas fa-redo me-2"></i> Email'i Tekrar Gönder
                    </button>
                </form>
            }
            
            <a asp-action="Login" class="btn btn-secondary email-sent-btn">
                <i class="fas fa-arrow-left me-2"></i> Giriş Sayfasına Dön
            </a>
            
            <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary email-sent-btn">
                <i class="fas fa-home me-2"></i> Anasayfaya Git
            </a>
        </div>

        <hr class="my-4">
        
        <div class="text-muted small">
            <i class="fas fa-info-circle me-1"></i> 
            Email almadıysanız, yukarıdaki "Email'i Tekrar Gönder" butonunu kullanabilirsiniz.
        </div>
    </div>
</div>
